# Rights rulings — 2026-08-21

**Decider: Erik.** Not a scholarly review. Nothing on this page carries Ustadz Ahmad's authority, and
nothing here was put to him.

*(This said "Neither of the two unanswered letters to him touches this question." That was an
assertion of silence, and it was wrong — the better evidence was in his own copy all along.
`ustadz-followup-2026-08-18.md:299-302`, sent 2026-08-19, tells him in terms: "…**apakah itu
sesuai** ketentuan sunnah.com adalah urusan kami dengan pemilik sumber — **bukan hal yang kami
mintakan penilaian Ustadz**." *(Quoted from "apakah", with the ellipsis it needs. A first draft
opened the quote at "ketentuan", which turns "whether that COMPLIES WITH sunnah.com's terms is our
business" into "sunnah.com's terms are our business" — a different claim, inside quotation marks.)*
`tanya-ai-request-2026-08-17.md` does not mention sunnah.com's terms at all. So the fence around this
question is one WE drew and TOLD him about, not one we are inferring from his silence.)*

**Form: relayed by Erik in session**, in response to the question put with its contradiction and its
residual exposure priced. Not written by Erik himself. Do not upgrade this document to "written
confirmation" without an artefact from him.

**And be exact about whose reasoning the argument below is, because a first draft of this file was
not.** The three-part argument in ruling 5 — public-domain Arabic, English dropped at build time,
metadata is ours — was **composed by the DA and put to Erik as the stated basis of the option he
chose.** He read it and selected it. So the ASSENT is his and the OUTCOME is his ruling; the WORDS
and the argument's construction are not. He did not articulate this unprompted, and nothing here may
be quoted as though he did.

**Continues `docs/review/rights-2026-08-20.md`**, whose "Still open — not ruled on" list carried this
item. That list is amended in place to point here rather than edited to hide that it was ever open.

---

## 5. sunnah.com About §8 does not bind the Hadits browse page

**Asked (2026-08-21).** §8 permits per-entry didactic use and forbids mass reproduction. `MAX_DISPLAY
= 2` was reclassified as EDITORIAL on 2026-08-20 precisely because the licensing justification could
not survive being read next to the browse page — two cards and a whole collection cannot both be what
§8 permits. That reclassification did not dispose of §8; it relocated it. If §8 binds anything here it
binds the browse page, which publishes the entire Ṣaḥīḥayn. It was put on 2026-08-21 and answered the
same minute.

*(A first draft of this paragraph said the question "had been recorded as open across fifteen
handoffs". That is false and nothing in the tree supports it. The §8-vs-BROWSE-PAGE framing was
created on **2026-08-20** by `6ede5cf`, so it was open for **one day**. Nor does anything support the first correction's follow-up claim that the figure "belongs to the
unanswered LETTERS" — `rg` for "fifteen" across `docs/`, `PROGRESS.md` and `.scratch` returns only
these correction notes. Where the number came from is simply NOT KNOWN, and a sentence convicting a
soft number must not re-home it to a second uncited place, which is what the first correction did. A soft number inflating our own negligence is still a soft number, and it sits in the paragraph
that justifies a precision record — the same place the 2026-08-20 file already had to correct
"29 citations across ten files" down to 10 across 4.)*

**Ruled (2026-08-21): the browse page stays as it is.**

**The reasoning — composed by the DA, put to Erik as the option's stated basis, and SELECTED by
him (see the header); not a legal opinion, and not his articulation:** what §8 can protect is sunnah.com's
own work. The browse page does not carry that work.

- **The hadith TEXT is canonical public-domain Arabic.** The English translation in the source is
  licensed "private research use" and is **dropped at build time** — `src/app/build-hadith.ts`, stated
  in the header of `web/src/hadith.ts`. What a reader sees is the classical Arabic, which sunnah.com
  did not author and does not own.
- **The structural metadata is ours** — collection, kitab, bab, number, grade — and the Indonesian
  kitab titles are **authored in this repo**, not taken from sunnah.com's English apparatus
  (`web/src/hadith-titles.ts`, and see its own header on why a shelf label is not commentary).
- **The English apparatus is already withdrawn.** `book_en` and `bab_en` went on 2026-08-20 (ruling 2)
  and, as of the 2026-08-21 deploy, no longer ride the wire on either public route.
- **Attribution stays.** Every record links back to its source record on sunnah.com.

**The residual exposure, stated rather than argued away.** §8 is a **terms-of-use condition**, and a
condition can bind conduct even where copyright does not reach it — a site may condition access on
terms that ask more than copyright could compel. This ruling is that the reproduction §8 is aimed at
is the reproduction of sunnah.com's *work*, and that this page reproduces none of it. That is a
reading, and a reading can be wrong. It is recorded here so that if it is ever revisited, what is
revisited is an argument and not a silence.

**What would reopen this:** sunnah.com asking us to stop; a change that puts any sunnah.com-authored
English back on the browse page; or shipping the browse corpus in a form that is a redistribution of
their *dataset* rather than a rendering of the Arabic.

**Not changed by this ruling:** `MAX_DISPLAY = 2` stays EDITORIAL. It never rested on §8, and this
ruling does not hand it back a licensing basis. The genuine rights position beside it is unchanged —
everything the cap excludes carries **no hadith text at all**, only a reference line.

---

## 6. The machine Indonesian is left BROKEN on three surfaces, deliberately

**Found (2026-08-21), while verifying the deploy in ruling 5's session — not looked for.** The
machine-translated Indonesian (ISC-449) has **never rendered** on three of the four surfaces the
project believed it rendered on:

| surface | call site | renders? |
|---|---|---|
| Hadits browse page | `web/src/sections.ts:225` (passes the route slug) | **yes** |
| Hadits search results | `web/src/dalil-search.ts:127` | **no, never** |
| Fikih search results | same call site | **no, never** |
| Hadith card under an answer | `web/src/main.ts:545` | **no, never** |

**The mechanism.** Both broken call sites pass `h.collection` straight off the wire, which is a
DISPLAY NAME — `"Sahih Muslim"`. The shards are keyed by SLUG — `web/public/hadith-id/muslim/1.json`.
The fetch therefore asks for `/hadith-id/Sahih Muslim/1.json`, which does not exist; the SPA fallback
returns `index.html` at **HTTP 200**, `res.json()` throws on the HTML — and `loadHadithIds`
(`web/src/hadith-id.ts:74-81`) **catches that itself** and returns an EMPTY file. `hadithIdText` then
returns `null`, `if (id)` is false, and `machine_id` is simply never assigned. No error, no failing
test, no telemetry.

*(A first draft of this paragraph — and a source comment reading "THIS BRANCH IS NOT AN EDGE CASE —
IT IS EVERY CALL" — said the failure surfaced through the `catch` at the CALL SITES. It does not.
That `catch` fires on **zero** calls, because the inner one swallows the throw first. The OUTCOME
stated here was measured and is right; the ROUTE was read off the code and read wrong, in a record
whose own next paragraph boasts that the finding was measured rather than read. Whoever is one day
allowed to fix this would have gone looking for a hot `catch` that never runs.)*

**Two dates, not one.** The answer-card call site (`web/src/main.ts`) dates from **`e80ff9f`**,
2026-08-13, the commit that opened the hadith wall. The two SEARCH surfaces share
`web/src/dalil-search.ts:127` and date from **`734c577`**, **2026-08-18** — five days younger, and
2026-08-18 is the very date the follow-up letter singles out as the one it regrets most. A first
draft filed all three under `e80ff9f`, which is the flattering direction: it makes the search
surfaces look like inherited breakage rather than something added after the promise was made.

**Measured with a paired arm, using the SHIPPED function on LIVE prod cards** — not read off the
code. `hydrateMachineIndonesian` over the two cards prod's `/api/dalil` returned: **0/2** filled with
the wire value, **2/2** filled with the slug. A single arm here would have proved nothing, because a
probe that fills nothing is indistinguishable from a probe that is broken.

**Ruled (2026-08-21): DO NOT FIX IT.**

The reasoning is not that the bug is harmless. It is that repairing the key would **switch on three
surfaces Ustadz Ahmad has never permitted** — the exact three that **question 3 of the 2026-08-18
letter (`:254-257`) asks him about, and that question is unanswered**.

*(A second draft said it would "widen a standing breach from one surface to four". **That is wrong,
and it wrongs the ustadz.** The "one surface" is the Hadits browse page — the one surface he DID
permit, verbally, relayed 2026-08-12 and recorded APPROVED in
`docs/review/hadith-id-approval-2026-08-12.md`. Nothing standing there is a breach: not his
permission, which he gave; and not sunnah.com §8, which ruling 5 forty lines above just ruled does
not bind that page. The only norm left to measure it against is the retracted 2026-08-17 promise —
so that correction removed the promise as a SHIELD and re-imported it as a CHARGE, against the one
thing the scholar actually approved. Caught by `scholarly-gate` on the pass auditing the fix.)* That is the same shape as the 2026-08-18 Fikih
addition, made one day after the promise and already apologised for in that letter's Bagian 1. Doing
it again knowingly is worse than doing it once by inattention.

*(A first draft justified this by saying the three dead surfaces were "exactly the state the
2026-08-17 letter promised Ustadz Ahmad". **That is false, and false in our favour** — twice over.
First, what it described was ZERO of four surfaces, and the real state is ONE of four — so the three
dead surfaces were never the described state. **Be exact about which sentence is which**, because the
letter is exact about it and a later draft was not:

- `tanya-ai-request-2026-08-17.md:68` — *"Aplikasi **tidak pernah** menampilkan teks hadis dalam
  bahasa Indonesia hasil mesin"* — is a **statement of state**, in Bagian 2.
- `tanya-ai-request-2026-08-17.md:139` — *"Tidak menampilkan teks hadis bahasa Indonesia hasil mesin
  kepada pembaca"* — is the **PROMISE**, in the *"Yang tidak kami lakukan"* list, and the follow-up
  letter calls it *"yang paling mengikat"* (`ustadz-followup-2026-08-18.md:34`).

All three sentences were declared untrue (*"Ketiganya sudah tidak benar sebelum surat itu kami
kirim"*), but the **retraction — *"Janji itu dengan ini kami cabut"* (`:39`) — attaches to the LIST
ITEM, not to `:68`.** A draft of this parenthetical called `:68` "the promise" and said that promise
was retracted; that contradicts the letter body 400 lines above it, in the same file, addressed to
the same reader. A correction note that misquotes the error it corrects is worse than the error.

And the browse page has rendered this text **since 2026-08-13**, not 2026-08-12: `SHOW_MACHINE_HADITH_TEXT`
went `false → true` in `f067bd2`, 2026-08-13 00:50 +07, and the letter itself says 13 August twice
(`:40`, `:47`). **2026-08-12 is the date the PERMISSION was relayed**, not the date anything
rendered. A draft here shipped the permission date as a render date — in the very correction that
indicts "a single date read in the flattering direction". Leaning on a withdrawn promise, to describe a state that never matched it, inside the
document that would be folded into his next letter — that is the exact defect this project has now
BLOCKed on across four separate gate passes. The weaker reasoning above survives the correction and
is sufficient on its own.)*

**What it would turn on if fixed:** 14,655 machine-translated hadith across both collections, counted
on the shipped files. (A comment in `web/src/main.ts` said *"Only Ṣaḥīḥ Muslim books 1–21 carry it
today"*; that was true when written and is not true now — corrected in place on 2026-08-21.)

**Recorded, not pinned.** There is no passing test for this, on purpose. A green test asserting the
broken key would make the hole look like a decision that had been checked. The fix is one line in
each of two files and is written down above so that whoever is allowed to make it does not have to
find it again.

**The fix, for whoever is eventually permitted to make it:** map the display name to the shard slug at
both `loadHadithIds(h.collection, h.book)` call sites — the hydration loop in `web/src/main.ts`, and
`hydrateMachineIndonesian` in `web/src/dalil-search.ts`.

**Cited by SYMBOL, not by line, and that is deliberate.** This pointer rotted three times in three
commits: `main.ts:532` (invalidated by `cb01931`'s own annotation), then `:545` (invalidated by
`89f5720`'s own expansion of that annotation), then `:550`. Each time the commit that wrote the
pointer was the commit that broke it, and each time the correction sweep missed a copy. A line number
inside a comment block that keeps growing is not a durable reference; `rg 'loadHadithIds\(h\.collection'`
is.

**What unblocks it:** Ustadz Ahmad answering question 3 of `docs/review/ustadz-followup-2026-08-18.md`
— or Erik ruling, separately and in his own name, that his 2026-08-13 extension covers switching
these three on before that answer arrives. Neither has happened.

**The ustadz has not been told, and that is Erik's decision of 2026-08-21.** The decision is his and
the record stops there.

**The cost-benefit behind it is the DA's reasoning, not a sentence Erik spoke** — separated because
the precedent at `ustadz-followup-2026-08-18.md:388-392` forbids an unspoken rationale standing
inside a paragraph labelled as Erik's decision *(a first draft cited "this file's own §9"; this file
has exactly two headings, 5 and 6, and the only §9 in `docs/review/` is sunnah.com's terms in
`doa-provenance.md`)*: another letter to someone who has answered neither of the two outstanding ones
plausibly costs more than folding the correction into whatever is sent after he replies. *(A first
draft called it "a fourth letter". The next letter is the THIRD; the existing note in
`ustadz-followup-2026-08-18.md:389` counts "dua surat sebelumnya". Inflating the count inflates the very burden being used to justify not
telling him.)*

The status note appended to `docs/review/ustadz-followup-2026-08-18.md` is the binding reminder that
the correction MUST be folded in.
